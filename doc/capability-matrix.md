# Capability Matrix

This matrix defines the target Go implementation. The current Go workspace already contains
scaffolded slices, so the `Status` column reflects whether each slice is still planned or has landed
as a scaffold.

| Capability | Level | Phase | Status | Notes |
| --- | --- | --- | --- | --- |
| Public facade scaffold | Stable Core | 0 | Scaffolded | `Harness`, `HarnessBuilder`, `Error`, `DebugView`, and `MockRegistryView` stay thin and delegate into internal packages. |
| Harness constructors | Stable Core | 0 | Scaffolded | `FromHTML`, `FromHTMLWithURL`, `FromHTMLWithLocalStorage`, `FromHTMLWithURLAndLocalStorage`, `FromHTMLWithSessionStorage`, and `FromHTMLWithURLAndSessionStorage` build the initial session snapshot through the builder. |
| Builder configuration capture | Stable Core | 0 | Scaffolded | URL, HTML, local/session storage, navigator online state, random seed, matchMedia rules, and open/close/print/scroll failure seeds are explicit builder fields. |
| Error taxonomy | Stable Core | 0 | Scaffolded | The package classifies HTML parse, script parse/runtime, selector, DOM, event, timer, mock, assertion, and unsupported failures. |
| Harness accessors and views | Stable Core | 0 | Scaffolded | `URL`, `HTML`, `NowMs`, `Debug`, and `Mocks` return session snapshots or views; `DebugView` includes read-only observation of configured builder state such as `RandomSeed`, `NavigatorOnLine`, `MatchMediaRules`, and the failure seed readouts `OpenFailure` / `CloseFailure` / `PrintFailure` / `ScrollFailure`, plus live inspection helpers such as `DumpDOM`, `NodeCount`, `ScriptCount`, `ImageCount`, `FormCount`, `SelectCount`, `TemplateCount`, `TableCount`, `ButtonCount`, `TextAreaCount`, `InputCount`, `FieldsetCount`, `LegendCount`, `OutputCount`, `LabelCount`, `ProgressCount`, `MeterCount`, `AudioCount`, `VideoCount`, `IframeCount`, `EmbedCount`, `TrackCount`, `StrongCount`, `SpanCount`, `DataCount`, `DfnCount`, `KbdCount`, `SampCount`, `VarCount`, `FocusedSelector`, `FocusedNodeID`, `TargetNodeID`, `HistoryLength`, `HistoryState`, `HistoryIndex`, `HistoryEntries`, `VisitedURLs`, `HistoryScrollRestoration`, `PendingTimers`, `PendingAnimationFrames`, `PendingMicrotasks`, `ScrollPosition`, `WindowName`, `Clipboard`, `LocalStorage`, `SessionStorage`, `DocumentCookie`, `CookieJar`, `NavigationLog`, `Interactions`, `EventListeners`, the fetch call trace, fetch rule traces, dialog message traces, download/file-input traces, storage change traces, browser-action call traces, matchMedia query traces, matchMedia listener traces, clipboard write traces, pending timer/animation-frame traces, and location parts (`LocationOrigin`, `LocationProtocol`, `LocationHost`, `LocationHostname`, `LocationPort`, `LocationPathname`, `LocationSearch`, `LocationHash`), while time and user-like actions are covered by later runtime rows. |
| DOM readiness snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.DOMReady()` and `DOMError()` expose DOM initialization readiness and the most recent DOM parse/runtime failure text as read-only inspection data. |
| Inline script trace snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.LastInlineScriptHTML()` exposes the most recently executed classic inline script outerHTML as read-only inspection data. |
| Builder HTML snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.InitialHTML()` exposes the original builder HTML input as read-only inspection data without bootstrapping the DOM. |
| Select count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.SelectCount()` exposes the current select count as read-only inspection data. |
| Template count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.TemplateCount()` exposes the current template count as read-only inspection data. |
| Table count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.TableCount()` exposes the current table count as read-only inspection data. |
| Button count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.ButtonCount()` exposes the current button count as read-only inspection data. |
| Textarea count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.TextAreaCount()` exposes the current textarea count as read-only inspection data. |
| Fieldset count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.FieldsetCount()` exposes the current fieldset count as read-only inspection data. |
| Legend count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.LegendCount()` exposes the current legend count as read-only inspection data. |
| Output count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.OutputCount()` exposes the current output count as read-only inspection data. |
| Label count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.LabelCount()` exposes the current label count as read-only inspection data. |
| Progress count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.ProgressCount()` exposes the current progress count as read-only inspection data. |
| Meter count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.MeterCount()` exposes the current meter count as read-only inspection data. |
| Audio count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.AudioCount()` exposes the current audio count as read-only inspection data. |
| Video count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.VideoCount()` exposes the current video count as read-only inspection data. |
| Iframe count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.IframeCount()` exposes the current iframe count as read-only inspection data. |
| Embed count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.EmbedCount()` exposes the current embed count as read-only inspection data. |
| Track count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.TrackCount()` exposes the current track count as read-only inspection data. |
| Picture count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.PictureCount()` exposes the current picture count as read-only inspection data. |
| Source count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.SourceCount()` exposes the current source count as read-only inspection data. |
| Dialog count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.DialogCount()` exposes the current dialog count as read-only inspection data. |
| Details count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.DetailsCount()` exposes the current details count as read-only inspection data. |
| Summary count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.SummaryCount()` exposes the current summary count as read-only inspection data. |
| Section count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.SectionCount()` exposes the current section count as read-only inspection data. |
| Main count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.MainCount()` exposes the current main count as read-only inspection data. |
| Article count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.ArticleCount()` exposes the current article count as read-only inspection data. |
| Nav count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.NavCount()` exposes the current nav count as read-only inspection data. |
| Aside count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.AsideCount()` exposes the current aside count as read-only inspection data. |
| Figure count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.FigureCount()` exposes the current figure count as read-only inspection data. |
| Figcaption count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.FigcaptionCount()` exposes the current figcaption count as read-only inspection data. |
| Header count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.HeaderCount()` exposes the current header count as read-only inspection data. |
| Footer count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.FooterCount()` exposes the current footer count as read-only inspection data. |
| Address count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.AddressCount()` exposes the current address count as read-only inspection data. |
| Blockquote count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.BlockquoteCount()` exposes the current blockquote count as read-only inspection data. |
| Paragraph count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.ParagraphCount()` exposes the current paragraph count as read-only inspection data. |
| Pre count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.PreCount()` exposes the current pre count as read-only inspection data. |
| Mark count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.MarkCount()` exposes the current mark count as read-only inspection data. |
| Q count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.QCount()` exposes the current q count as read-only inspection data. |
| Cite count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.CiteCount()` exposes the current cite count as read-only inspection data. |
| Abbr count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.AbbrCount()` exposes the current abbr count as read-only inspection data. |
| Strong count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.StrongCount()` exposes the current strong count as read-only inspection data. |
| Span count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.SpanCount()` exposes the current span count as read-only inspection data. |
| Data count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.DataCount()` exposes the current data count as read-only inspection data. |
| Dfn count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.DfnCount()` exposes the current dfn count as read-only inspection data. |
| Kbd count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.KbdCount()` exposes the current kbd count as read-only inspection data. |
| Samp count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.SampCount()` exposes the current samp count as read-only inspection data. |
| Ruby count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.RubyCount()` exposes the current ruby count as read-only inspection data. |
| Rt count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.RtCount()` exposes the current rt count as read-only inspection data. |
| Var count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.VarCount()` exposes the current var count as read-only inspection data. |
| Code count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.CodeCount()` exposes the current code count as read-only inspection data. |
| Small count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.SmallCount()` exposes the current small count as read-only inspection data. |
| Time count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.TimeCount()` exposes the current time count as read-only inspection data. |
| Option label snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.OptionLabels()` exposes the current option label snapshot as read-only inspection data. |
| Selected option label snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.SelectedOptionLabels()` exposes the current selected option label snapshot as read-only inspection data. |
| Option value snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.OptionValues()` exposes the current option value snapshot as read-only inspection data. |
| Selected option value snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.SelectedOptionValues()` exposes the current selected option value snapshot as read-only inspection data. |
| Option count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.OptionCount()` exposes the current option count as read-only inspection data. |
| Selected option count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.SelectedOptionCount()` exposes the current selected option count as read-only inspection data. |
| Optgroup count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.OptgroupCount()` exposes the current optgroup count as read-only inspection data. |
| Link count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.LinkCount()` exposes the current link count as read-only inspection data. |
| Anchor count snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.AnchorCount()` exposes the current anchor count as read-only inspection data. |
| Optgroup label snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.OptgroupLabels()` exposes the current optgroup label snapshot as read-only inspection data. |
| History stack snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.HistoryEntries()` exposes the current history stack as a read-only inspection slice, and `DebugView.HistoryIndex()` exposes the current history cursor as a read-only inspection integer. |
| Timer queue snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.PendingTimers()` and `PendingAnimationFrames()` expose scheduled timer and animation-frame snapshots as read-only inspection slices. |
| Microtask queue snapshot | Experimental Browser Facades | 1 | Scaffolded | `DebugView.PendingMicrotasks()` exposes queued microtask sources as a read-only inspection slice. |
| DOM store and HTML parsing | Stable Core | 1 | Scaffolded | Generational node IDs, tree storage, parsing, and DOM dump helpers now live in `internal/dom`; raw `<script>` text is preserved so inline script sources can round-trip through the parser and serializer. |
| Selector subset and assertions | Stable Core | 1 | Scaffolded | The bounded selector slice (`tag`, `#id`, `.class`, `*`, bounded attribute selectors such as `[attr]`, `[attr=value]`, `[attr~=value]`, `[attr|=value]`, `[attr^=value]`, `[attr$=value]`, and `[attr*=value]`, simple combinations, comma-separated selector lists, bounded descendant/child/sibling combinators, and a bounded pseudo-class slice such as `:root`, `:scope`, `:defined`, `:state(identifier)`, `:active`, `:hover`, `:empty`, `:checked`, `:indeterminate`, `:autofill`, `:-webkit-autofill`, `:default`, `:enabled`, `:disabled`, `:required`, `:optional`, `:read-only`, `:read-write`, `:valid`, `:invalid`, `:user-valid`, `:user-invalid`, `:in-range`, `:out-of-range`, `:first-child`, `:last-child`, `:first-of-type`, `:last-of-type`, `:only-child`, `:only-of-type`, `:nth-child()` / `:nth-last-child()` with bounded `of selector-list` filters, `:nth-of-type()`, `:nth-last-of-type()`, `:link`, `:any-link`, `:visited`, `:local-link`, `:lang()`, `:dir()`, `:placeholder-shown`, `:blank` (text-like inputs, textareas, unchecked checkable controls, and empty selects), `:heading`, `:heading(integer#)`, `:playing`, `:paused`, `:seeking`, `:buffering`, `:stalled`, `:muted`, `:volume-locked`, `:modal`, `:popover-open`, `:open` (details/dialog plus select/input picker approximations), `:focus`, `:focus-visible`, `:focus-within`, `:target`, `:target-within`, `:is()` / `:where()` / `:not()` with forgiving selector lists, and `:has()` with forgiving child-relative and sibling-relative selectors) exists in `internal/dom`; document queries treat `:scope` as the document root scope, while element-level `Matches` and `Closest` use the element itself as scope and element-bound `querySelector` / `querySelectorAll` search descendants only; custom element states are approximated with a tokenized `state` attribute on custom elements; `:blank` is approximated for text-like inputs and textareas with empty or whitespace-only values, unchecked checkable inputs, and selects whose current value is empty; `:local-link` is approximated as a same-document link against the current session URL; `:visited` is approximated against the current session history URLs; public `Harness` assertion helpers (`AssertText`, `AssertValue`, `AssertChecked`, and `AssertExists`) now delegate to the same selector engine and include DOM dumps on failure. |
| Document property bridge | Stable Core | 2 | Scaffolded | Bounded `document` property reads for `title`, `readyState`, `activeElement`, `baseURI`, `URL`, `doctype`, `documentURI`, `defaultView`, `compatMode`, `contentType`, `designMode`, and `dir` are exposed through the runtime bridge; `readyState` reports `loading` during inline-script bootstrap and `complete` after bootstrap, `activeElement` follows the current focus snapshot, `defaultView` reuses the `window` bridge, `doctype` is `null` in the current bounded HTML slice, and the remaining string-valued properties map to the current session URL or bounded document state. |
| Node / Element tree-navigation bridge | Stable Core | 2 | Scaffolded | Bounded `document` and `element:` host refs expose `nodeType`, `nodeName`, `nodeValue`, `ownerDocument`, `parentNode`, `parentElement`, `firstChild`, `lastChild`, `firstElementChild`, `lastElementChild`, `nextSibling`, `previousSibling`, `nextElementSibling`, `previousElementSibling`, and `childElementCount` through the runtime bridge; tree navigation stays read-only and keeps the bounded DOM model explicit. |
| Equality operators | Stable Core | 2 | Scaffolded | Bounded equality operators `==`, `!=`, `===`, and `!==` work across bounded values, including object and array alias identity plus scalar coercion. |
| `in` / `instanceof` / `for...of` / `for...in` / `import()` options errors | Stable Core | 2 | Scaffolded | Bounded relational `in` operators now fail with runtime errors on non-object right-hand values, bounded relational `instanceof` operators now fail with runtime errors on non-class right-hand values, bounded `for...of` loops now fail with runtime errors on non-iterable right-hand values, bounded `for...in` loops now fail with runtime errors on non-object right-hand values, and dynamic `import()` optional attributes now fail with runtime errors when the attributes value is not an object instead of generic unsupported errors. |
| Module metadata `import.meta` | Stable Core | 2 | Scaffolded | Classic scripts now reject `import.meta` syntax outside module scripts with a parse error instead of falling through to a generic unsupported error, while bounded module scripts expose `import.meta` as an object with a `url` property. |
| Bare dynamic import expressions | Stable Core | 2 | Scaffolded | The classic-JS parser also accepts bare `import(...)` expression statements and routes them through the same bounded dynamic-import path as `await import(...)`. |
| Bounded browser stdlib slice | Stable Core | 2 | Scaffolded | Inline classic-JS scripts expose a bounded browser stdlib surface for `Array` / `Object` / `JSON` / `Number` / `String` / `Boolean` / `Math` / `Date`, including the template-facing `Array.from()` / `Array.isArray()`, `Object.assign()` / `Object.keys()` / `Object.entries()` / `Object.values()`, `JSON.parse()` / `JSON.stringify()`, `Number.isFinite()` / `Number.NaN`, `Math.abs()` / `Math.min()` / `Math.max()` / `Math.random()`, `Date.now()`, `Intl.DateTimeFormat()`, `String.prototype.indexOf()` / `String.prototype.startsWith()` / `String.prototype.endsWith()`, `Array.prototype.findIndex()` / `splice()` / `unshift()`, `Number.prototype.toPrecision()` / `toExponential()`, the bounded array/string/number/date prototype helpers used by template-driven bootstrap, the live `URL` / `URLSearchParams` query-state bridge (`search`, `searchParams.set()`, `searchParams.getAll()`, `searchParams.entries()`, `searchParams.values()`, `searchParams.sort()`, `searchParams.keys()`, `forEach()`), and bounded promise-style `then()` / `catch()` chains on browser promises such as `clipboard.writeText()`. |
| `with` statements | Stable Core | 2 | Scaffolded | Classic-JS inline scripts now accept bounded `with (expr) statement` bodies when the expression evaluates to an object or array value, and identifier lookup / assignment inside the body resolve through that bounded with scope before falling back to outer lexical bindings. |
| Re-export import attributes | Stable Core | 2 | Scaffolded | Re-export syntax with `from` also accepts optional import attributes / options objects on bounded module scripts, including namespace re-exports and default + namespace import combinations. |
| Anonymous default-exported plain functions | Stable Core | 2 | Scaffolded | Module scripts also accept anonymous `export default function () {}` forms in the same bounded default-export slice as class, async function, and async generator forms. |
| Tagged template callability | Stable Core | 2 | Scaffolded | Tagged template literals still accept bounded function tags and interpolation, and non-callable tags now fail with runtime errors instead of generic unsupported errors. |
| Reserved declaration names parse errors | Stable Core | 2 | Scaffolded | Reserved declaration names such as `this`, `void`, `function`, `class`, and `debugger` now reject with parse errors in declaration positions and binding patterns instead of falling through to generic unsupported errors. |
| Static class members named `prototype` | Stable Core | 2 | Scaffolded | Static class members named `prototype` are supported in the bounded class slice while class instantiation still uses the hidden internal class prototype slot, including setter-only `prototype` members that read back as `undefined`. |
| Nested `yield*` delegation | Stable Core | 2 | Scaffolded | Bounded generator block bodies now allow nested `yield*` delegation over string, array, and iterator-like object values, surface iterator final return values when delegation completes, fail with runtime errors for scalar inputs, and resume through the same continuation stack as nested `yield`. |
| Unary `+` / `-` / `void` on bounded scalar values | Stable Core | 2 | Scaffolded | Unary `+` / `-` / `void` now coerce bounded scalar values in the classic-JS slice, while `+BigInt` remains an explicit unsupported error in this bounded slice. |
| `extends null` class inheritance | Stable Core | 2 | Scaffolded | `class Example extends null {}` is accepted in the bounded class slice and still instantiates with the same bounded class object semantics. |
| Call argument spread | Stable Core | 2 | Scaffolded | Bounded call argument spread accepts string values, array values, and iterator-like objects with a `next()` method. Array spread and array destructuring also accept string values and iterator-like objects with a `next()` method. |
| Object spread primitive no-op | Stable Core | 2 | Scaffolded | Bounded object spread treats `null` and `undefined` as no-op spreads, and also treats other primitive values as no-op spreads while still accepting string, array, and object values. |
| Array literal elisions | Stable Core | 2 | Scaffolded | Bounded array literals accept elisions as undefined slots in this bounded slice, so `[ , 1, , ]` evaluates as a 3-element array with bounded undefined entries. |
| Default + namespace module imports | Stable Core | 2 | Scaffolded | `import seeded, * as ns from ...` is supported in the bounded module syntax slice, and still binds the default export plus the module namespace object explicitly. |
| Using declarations in for headers | Stable Core | 2 | Scaffolded | `using` declarations are supported in bounded `for...of` and `for...in` headers, and `await using` declarations are supported in bounded `for await...of` headers; all keep the same immutable binding behavior as the rest of the `using` slice. |
| Object literal async / generator methods | Stable Core | 2 | Scaffolded | Bounded object literals also accept `async` methods, generator methods, and async generator methods with the same call semantics as the rest of the classic-JS slice. |
| Object literal private-name method rejection | Stable Core | 2 | Scaffolded | Private names inside object literal methods now reject with parse errors in this bounded slice instead of falling through to generic unsupported errors. |
| Malformed object literal shorthand parse errors | Stable Core | 2 | Scaffolded | Malformed object literal shorthand sequences now reject with parse errors in this bounded slice instead of falling through to generic unsupported errors. |
| Object literal `super` methods | Stable Core | 2 | Scaffolded | Bounded object literal methods can read `super` through bounded prototype targets, including inherited prototype objects and the default object-literal prototype fallback. |
| Object literal `super` null-prototype access | Stable Core | 2 | Scaffolded | Bounded object literal methods can also read `super` through bounded null-prototype object literals, where `super` property access resolves to `undefined` and call expressions now surface bounded runtime errors. |
| Object literal `super` null-prototype assignment | Stable Core | 2 | Scaffolded | Bounded object literal methods can also write `super` through bounded null-prototype object literals, including compound assignments that update the receiver object, while call-style `super` continues to surface a bounded runtime error. |
| Object literal and class `super` delete | Stable Core | 2 | Scaffolded | Bounded object literal and class methods can delete `super` through bounded prototype targets and bounded null-prototype object literals, updating the target or receiver object deterministically. |
| `super` outside bounded methods | Stable Core | 2 | Scaffolded | `super` outside bounded class and object literal methods now rejects with parse errors instead of falling through to generic unsupported errors. |
| Class expressions | Stable Core | 2 | Scaffolded | Bounded class expressions can be anonymous or named, reuse the bounded class-declaration machinery without leaking the class binding into the outer scope, can be instantiated directly with bounded `new` expressions, and can be used as bounded `extends` bases, including class-valued expressions returned from bounded functions and bounded constructible function values with bounded `.prototype` access and class field initializers / computed member names that can read `super`. |
| Class constructor args | Stable Core | 2 | Scaffolded | `new Class(...args)` now forwards constructor arguments into bounded class constructors. |
| Comma operator / sequence expressions | Stable Core | 2 | Scaffolded | Classic-JS expressions accept `,` as a sequence operator, while array/object literals and call arguments still treat commas as separators. |
| Event dispatch and default actions | Stable Core | 3 | Scaffolded | User-like actions (`Click`, `Focus`, `Blur`, `TypeText`, `SetChecked`, `SetSelectValue`, and `Submit`) validate selectors, track focus, dispatch bounded capture/target/bubble listeners registered from inline scripts through the host bridge or standard `window` / `document` / `element` `addEventListener` surfaces for click/input/change/submit/keyboard sequences, allow `host:preventDefault()` in listener sources to suppress click/reset default actions, allow `host:stopPropagation()` to stop later propagation, allow `host:removeEventListener()` to unregister previously registered handlers, allow a bounded boolean `once` flag for one-shot listeners, expose a bounded `eventTargetValue()` helper for listener scripts to inspect the current event target value during dispatch, and expose bounded synthetic `Dispatch` / `DispatchKeyboard` helpers for custom and keyboard event sequences; the slice still applies bounded default actions (checkbox/radio toggles, reset-button form reset, hyperlink navigation, `_blank` open capture, or download capture for `a`/`area`, and submit-button form submission), captures an interaction log, and keeps `Focus`/`Blur` and reset target-only. |
| Form controls and selection state | Stable Core | 3 | Scaffolded | Text-like input and textarea values, checkbox/radio checkedness (including radio-group exclusivity), and select option selectedness can be updated deterministically through user-like actions and are observable via `DebugView.DumpDOM()`. |
| Deterministic scheduler and time | Stable Core | 4 | Scaffolded | Fake clock control is explicit in `internal/runtime` through `Scheduler` and `Session` time helpers; a bounded microtask queue is available through `host:queueMicrotask()` and drains at script/action boundaries, bounded timers are available through `host:setTimeout()` / `host:setInterval()` with `host:clearTimeout()` / `host:clearInterval()`, bounded animation-frame callbacks are available through `host:requestAnimationFrame()` / `host:cancelAnimationFrame()`, and all of them drain when `Harness.AdvanceTime()` is called. |
| Window history state | Stable Core | 4 | Scaffolded | Bounded history entries, length/state, and `scrollRestoration` live in `internal/runtime`; `window.history`-style host helpers cover `historyPushState`, `historyReplaceState`, `historyBack`, `historyForward`, `historyGo`, `historyLength`, `historyState`, `historyScrollRestoration`, and the writable `historySetScrollRestoration`, while location assign/replace/reload and location property setters feed the same navigation log. |
| Cookie jar and navigator cookie flag | Stable Core | 4 | Scaffolded | `document.cookie` is modeled as a bounded session cookie jar in `internal/runtime`, with host helpers for reading and writing the cookie string plus a true `navigator.cookieEnabled`-style flag. |
| Navigator online state | Stable Core | 4 | Scaffolded | `navigator.onLine` is modeled as session state in `internal/runtime`; `HarnessBuilder.NavigatorOnLine(false)` seeds offline bootstrap deterministically, and `DebugView.NavigatorOnLine()` reports the effective online/offline state plus whether it was explicitly configured. |
| Window name state | Stable Core | 4 | Scaffolded | `window.name` is modeled as session state in `internal/runtime`, with bounded host helpers for reading and writing the window name string that persists across navigation and document replacement. |
| Web storage state | Stable Core | 4 | Scaffolded | `localStorage` and `sessionStorage` are modeled as bounded session state in `internal/runtime`, with host helpers for get/set/remove/clear/length/key and deterministic rollback support; storage mutations are captured as an ordered change log through `Events()` with explicit `seed` / `set` / `remove` / `clear` operations. |
| Typed mock registry families | Stable Test Mocks | 4 | Scaffolded | Fetch, dialogs, clipboard, location, open, close, print, scroll, matchMedia, downloads, file input, and storage are modeled as separate families with seed, capture, failure, and reset semantics; the dialog family covers `confirm` / `prompt` response queues, location also covers current-URL updates from assign / replace / reload / property-set host helpers, `MatchMedia` listener capture can be injected through the registry for tests and its seeded rules are exposed through `Rules()`, and storage mutations are captured as an ordered change log with explicit `seed` / `set` / `remove` / `clear` operations. |
| Public mock actions on `Harness` | Stable Test Mocks | 4 | Scaffolded | Thin wrappers for `Fetch`, `Alert`, `Confirm`, `Prompt`, `ReadClipboard`, `WriteClipboard`, `MatchMedia`, `Open`, `Close`, `Print`, `ScrollTo`, `ScrollBy`, `Navigate`, `SetFiles`, and `CaptureDownload` delegate into the typed registry or runtime. |
| Selector expansion | Stable Core | 6 | Scaffolded | The bounded combinator slice has landed for descendant, child, and sibling relationships; bounded attribute selectors such as `[attr]`, `[attr=value]`, `[attr~=value]`, `[attr|=value]`, `[attr^=value]`, `[attr$=value]`, and `[attr*=value]` are also implemented, along with comma-separated selector lists, and a bounded pseudo-class slice (`:root`, `:scope`, `:defined`, `:state(identifier)`, `:active`, `:hover`, `:empty`, `:checked`, `:indeterminate`, `:autofill`, `:-webkit-autofill`, `:default`, `:enabled`, `:disabled`, `:required`, `:optional`, `:read-only`, `:read-write`, `:valid`, `:invalid`, `:user-valid`, `:user-invalid`, `:in-range`, `:out-of-range`, `:first-child`, `:last-child`, `:first-of-type`, `:last-of-type`, `:only-child`, `:only-of-type`, `:nth-child()` / `:nth-last-child()` with bounded `of selector-list` filters, `:nth-of-type()`, `:nth-last-of-type()`, `:link`, `:any-link`, `:visited`, `:local-link`, `:lang()`, `:dir()`, `:placeholder-shown`, `:blank` (text-like inputs, textareas, unchecked checkable controls, and empty selects), `:heading`, `:heading(integer#)`, `:playing`, `:paused`, `:seeking`, `:buffering`, `:stalled`, `:muted`, `:volume-locked`, `:modal`, `:popover-open`, `:open` (details/dialog plus select/input picker approximations), `:focus`, `:focus-visible`, `:focus-within`, `:target`, `:target-within`, `:is()` / `:where()` / `:not()` with forgiving selector lists, and `:has()` with forgiving child-relative and sibling-relative selectors) is also implemented, while broader pseudo-classes remain planned, guided by `html-standard/`; document queries treat `:scope` as the document root scope, while element-level `Matches` and `Closest` use the element itself as scope and element-bound `querySelector` / `querySelectorAll` search descendants only; custom element states are approximated with a tokenized `state` attribute on custom elements, and `:blank` is approximated for text-like inputs and textareas with empty or whitespace-only values, unchecked checkable inputs, and selects whose current value is empty, while `:local-link` is approximated as a same-document link against the current session URL and `:visited` is approximated against the current session history URLs, and `:enabled` / `:disabled` respect disabled fieldset and optgroup ancestry while disabled controls are ignored by the constraint-validation pseudo-classes; `:active` / `:hover` also include labeled controls via bounded label lookup, and `:default` keeps initial checked/selected snapshots for checkable controls and options. |
| Script DOM query APIs | Stable Core | 7 | Scaffolded | Host-bound query calls for `querySelector`, `querySelectorAll`, `matches`, and `closest` reuse the shared selector engine, accept comma-separated selector lists, and keep element-bound query calls descendant-only; `querySelectorAll` returns a minimal snapshot `NodeList` with bounded `forEach()` / `entries()` / `keys()` / `values()` parity; live `HTMLCollection` slices are tracked separately on `children`, `document.images`, `document.forms`, `form.elements`, `fieldset.elements`, `select.options`, `select.selectedOptions`, `datalist.options`, `table.rows`, `table.tBodies`, `HTMLTableSectionElement.rows`, `tr.cells`, `document.scripts`, `document.links`, and `document.anchors`, with live `NodeList` slices for `childNodes` and `template.content.childNodes`. |
| Live collections | Stable Core | 7 | Scaffolded | A bounded `HTMLCollection` slice covers `Element.children`, `document.children`, `document.images`, `document.forms`, `form.elements`, `fieldset.elements`, `select.options`, `select.selectedOptions`, `datalist.options`, `table.rows`, `table.tBodies`, `HTMLTableSectionElement.rows`, `tr.cells`, `document.scripts`, `document.links`, and `document.anchors`, and bounded live `NodeList` slices cover `document.childNodes` and `template.content.childNodes`, with live length/item lookup plus bounded `forEach()` / `entries()` / `keys()` / `values()` parity where applicable and snapshot `IDs()` helpers where applicable. |
| Attribute reflection, class, and dataset | Stable Core | 8 | Scaffolded | Bounded reflection helpers for `GetAttribute`, `HasAttribute`, `SetAttribute`, and `RemoveAttribute` are wired through the shared DOM attribute store, and public live `ClassList` / `Dataset` views now expose the same internal DOM slice through the facade. |
| Tree mutation and HTML serialization | Stable Core | 8 | Scaffolded | Selector-based public wrappers for `InnerHTML`, `TextContent`, `OuterHTML`, `SetInnerHTML`, `ReplaceChildren`, `CloneNode`, `SetTextContent`, `SetOuterHTML`, `InsertAdjacentHTML`, `RemoveNode`, and `WriteHTML` now delegate into the bounded DOM mutation slice; on `textarea`, content mutations that change its contents keep the reset default value in sync; `CloneNode()` duplicates the selected node and inserts the clone after the source. Inline-script host bridging also covers bounded element reflection reads and writes for `className`, `innerText`, `outerText`, `style`, `attributes`, and `classList`, plus `dataset` reads, writes, and deletes through the same surface; bounded `open`; and bounded low-level node-construction and selection helpers such as `createElement`, `createTextNode`, `appendChild`, `removeChild`, `replaceChild`, `insertAdjacentElement`, `insertAdjacentText`, `select()`, `window.confirm()`, `window.prompt()`, and `document.execCommand("copy")`. `WriteHTML()` covers the bounded document-write-style replay slice and rolls back DOM plus session state on failure. |
| Debug view | Experimental Browser Facades | 1 | Scaffolded | `DebugView` is read-only and intentionally limited to inspection data, including configured builder seed visibility, navigator online-state snapshots, match-media seed rules, the failure seed readouts `OpenFailure` / `CloseFailure` / `PrintFailure` / `ScrollFailure`, a live DOM dump (`DumpDOM()`), live node counts (`NodeCount()`), live script counts (`ScriptCount()`), live image counts (`ImageCount()`), live form counts (`FormCount()`), focus state, fragment target state, history state, history cursor, history stack snapshots, visited URL snapshots, history-scroll-restoration state, pending timer and animation-frame snapshots, pending microtask snapshots, clipboard text, web-storage snapshots, cookie-string and cookie-jar state, navigation-log state, fetch call trace, fetch rule traces, dialog message traces, download/file-input traces, storage change traces, browser-action call traces, matchMedia query traces, matchMedia listener traces, clipboard write traces, registered event listener snapshots, location parts, and interaction logs where that state is otherwise non-observable. |
| Hardening suite | Experimental Project | 5 | Scaffolded | Contract tests, subsystem tests, regression tests, and fuzz/property tests gate publication; the current workspace already has seeded fuzz/property coverage for the script, selector, timer/scheduler, location/history, cookie/window.name, and mock registry boundaries. |
| Rendering and layout engine | Out of Scope | N/A | Not Planned | No browser renderer or CSS layout engine is intended. |
| External network I/O | Out of Scope | N/A | Not Planned | Network behavior stays mock-driven. |

## Rules

- A capability does not become stable merely because code exists.
- Any new stable `Harness` API requires a matrix update before or with the implementation.
- Any new test-only mock must document seed state, capture behavior, failure injection, reset
  semantics, and a minimal usage example.
- Any feature that depends on HTML or DOM behavior must be checked against `../html-standard/`
  first.
- The capability matrix is the source of truth for what the Go workspace promises.
- Legacy and deprecated spec branches are not default targets; only current, bounded, user-visible
  behavior should be added unless the matrix explicitly calls out a compatibility exception.
